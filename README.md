# My Flutter Project

This is a reusable Flutter starter project for you all.

## 🔧 How to run


1. Get packages:

    flutter pub get
2. Run
    flutter run
3. clean
    flutter clean

4. this is how to Push it to GitHub

# 1. Init git repo
git init

# 2. Add all project files
git add .

# 3. Commit the first version
git commit -m "Initial commit with Flutter boilerplate"

# 4. Rename default branch to main
git branch -M main

# 5. Add remote repository (your GitHub repo)
git remote add origin https://github.com/AttendanceSys/attendance_system.git

# 6. Push to GitHub
git push -u origin main
